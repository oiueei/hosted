-r base.txt

# Exact pins, for the reason written at the top of base.txt: this is the file
# `pip-audit` runs against in CI *and* the one Heroku installs, so they must not
# be able to resolve differently.

# Database
psycopg2-binary==2.9.12
dj-database-url==2.3.0

# Production server
gunicorn==23.0.0
whitenoise==6.12.0

# Error monitoring (active only when SENTRY_DSN is set — see production.py).
# This deployment's service layer, not part of what OIUEEI distributes. Pinned
# like every other direct dependency so the Heroku build and pip-audit resolve
# the same version.
sentry-sdk==2.66.1
